# --- generation (Phase 2) ---
google-genai>=1.51.0

# --- rendering + validation (Phase 1 + 3 stage 1) ---
playwright>=1.45.0

# --- visual judge (Phase 3 stage 2) ---
# anthropic is optional — only needed if --provider anthropic. Pinned but uninstalled by default.
anthropic>=0.40.0 ; python_version >= "3.10"

# --- training prep + HF training (Phase 4+) ---
huggingface-hub>=0.27.0
datasets>=3.0.0
transformers>=4.55.0
# Note: peft, trl, accelerate, deepspeed are only needed on the HF training side
# and will be installed inside the HF Jobs container, not locally.

# --- misc ---
python-dotenv>=1.0.0
pillow>=10.0.0
httpx>=0.27.0
